# Installation

Getting started with Preinterpret is simple. Add it to your project's dependencies:

## Using cargo add

```bash
cargo add preinterpret
```

## Or manually in Cargo.toml

```toml
[dependencies]
preinterpret = "1.0"
```

## Next Steps

Now that you have preinterpret installed, let's [create your first macro](./first-macro.md)!
